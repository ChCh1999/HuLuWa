8/27-1
//to use the function of building a tower.
eg: auto(ArcherTowerLv1 *) aTower = new ArcherTowerLv1;
aTower->initTower();
aTower->setPosition(position);
addChild(aTower);

8/27-2
UpdateMenuIcon.cpp
BuildMenuPanel.cpp两个文件中由于防御塔还未写完，其中的条件判断还未补全。
